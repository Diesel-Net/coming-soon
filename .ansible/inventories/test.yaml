all:
  children:

    swarm_manager:
      hosts:
        test.diesel.net: 

  vars:
    proxy_domains: 
      - test.diesel.net
      - test.kiwi-labs.net
    
    ansible_user: automation
    ansible_python_interpreter: /usr/bin/python3
